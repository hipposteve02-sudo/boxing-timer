# Copy to .env and fill in. Any key left blank causes that AI platform to be
# skipped during the AI-discovery audit (the rest of the audit still runs).
# You can also run the AI module without any keys using `--mock`.

ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
PERPLEXITY_API_KEY=pplx-...
